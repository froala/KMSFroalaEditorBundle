{% if value is iterable %}JSON.parse( "{{ value | json_encode | raw | escape('js')  }}" ),
{% elseif value is same as(true) %}true,
{% elseif value is same as(false) %}false,
{% elseif value matches '/^\\d+$/' %}{{ value }},
{% elseif option == 'fontAwesomeTemplate' %}"{{ value|escape('js')|raw }}",
{% else %}"{{ value }}",
{% endif %}