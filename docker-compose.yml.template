version: '3.7'
services:
  ServiceName:
    restart: unless-stopped
    image: ImageName
    pull_policy: always
    networks:
      - caddy
    labels:
      caddy_0: UrlName
      caddy_0.reverse_proxy: "{{upstreams PortNum}}"

networks:
  caddy:
    external: true
